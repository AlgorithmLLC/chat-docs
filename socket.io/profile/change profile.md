### change profile(profile, ack)
```javascript
profile = {
  first_name: "Иван"
  , last_name: "Иванов"
}
```
Renames user.

Error types:
  - `wrong data provided`
  - `enter name and last name`
  - `something went wrong`

This frame's ack returns no data.
